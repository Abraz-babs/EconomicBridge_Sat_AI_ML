# EconomicBridge — ML Service requirements
#
# Dev shares the apps/api/.venv plus the ML libs below. Install with:
#   ..\api\.venv\Scripts\python.exe -m pip install -r apps/ml/requirements.txt

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.4,<3.0

# Database
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
greenlet>=3.0,<4.0

# Machine learning
scikit-learn>=1.5,<2.0
numpy>=1.26,<3.0
shap>=0.46,<1.0
joblib>=1.4,<2.0

# CropGuard ResNet-50 (Slice 5a+). torch + torchvision power real
# inference; Pillow handles JPEG/PNG decoding. The classifier
# gracefully degrades to a deterministic stub when these are missing
# (see models/crop_classifier.py) so tests can run without the 750MB
# torch install.
torch>=2.2,<3.0
torchvision>=0.17,<1.0
pillow>=10.0,<12.0

# Model-artifact fetch from S3 at startup (MODEL_S3_URI); creds via task role.
boto3>=1.34,<2.0
