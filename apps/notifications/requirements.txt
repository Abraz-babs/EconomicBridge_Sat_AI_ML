# EconomicBridge — Notifications Service requirements
# Dev shares apps/api/.venv (deps overlap). Prod gets its own Docker image.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.4,<3.0
httpx>=0.27,<1.0

# Database
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
greenlet>=3.0,<4.0

# Future: celery[redis] for outbox worker
