[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: tests that require a live database (run with `pytest -m integration`)
addopts = -m "not integration"
filterwarnings =
    ignore::DeprecationWarning:httpx.*
