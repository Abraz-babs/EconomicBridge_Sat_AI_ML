-r requirements.txt

# Test framework
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<7.0   # CI coverage reporting (Slice 15)

# In-memory AWS for S3 tests (test_s3_client.py + test_imagery_download.py)
# never touch real AWS in CI per CLAUDE.md §11.
moto>=5.0,<6.0
